# StatusBar (не реализовано)

StatusBar — планировавшийся компонент мониторинга состояния системы. В текущем коде TN_Doc **отсутствует** (нет исходников, сборки и маршрутов).

Если потребуется вернуть/реализовать StatusBar, стоит:
- описать API статусов в backend
- добавить UI (Razor или SPA)
- согласовать источники данных (SignalR/OPC)

