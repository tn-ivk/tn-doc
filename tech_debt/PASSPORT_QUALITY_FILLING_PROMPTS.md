## Промпты внедрения для плана заполнения паспорта качества

### Промпт 0 — Фаза 0: подготовка конфигов и feature‑flag

Хочу, чтобы ты реализовал **Фазу 0 — подготовка конфигов и feature‑flag** из файла `tech_debt/PASSPORT_QUALITY_FILLING_PLAN.md` для проекта `TN_Doc`.  
Работай в репозитории по пути `TN_Doc/` и строго следуй описанию Фазы 0 в разделе 11 плана.

**Требуется сделать:**

1. **Инвентаризация конфигов:**
   - Найди все конфиги вида `TN_Doc/Cfg/CfgEditPassport_*.json` (включая варианты для разных ГОСТ/устройств).
   - По `CfgApp.json` (и при необходимости другим конфигам) собери матрицу: `устройство ↔ UseElis (true/false)`.  
   - Зафиксируй эту матрицу в виде артефакта (например, `docs/configs/passport_devices_useElis.json` или `*.md`).

2. **Расширение эталонного конфига:**
   - В `TN_Doc/Cfg/CfgEditPassport_GOSTR50.2.040(I).json` добавь поле `IsBalast` для элементов `Parameters[]` по списку из раздела 4.1 плана (балластные/небалластные показатели).
   - Убедись, что отсутствие `IsBalast` по-прежнему трактуется как `false` кодом, который читает конфиги (никаких падений/исключений).

3. **Ручное обновление конфигов:**
   - На основе эталонного `CfgEditPassport_GOSTR50.2.040(I).json` и списка параметров из плана обнови `IsBalast` в актуальных `TN_Doc/Cfg/CfgEditPassport_*.json`:
     - используй существующие процессы управления конфигурациями (MR/PR, ручная правка),
     - не создавай отдельный миграционный скрипт.
   - Для каждого изменённого файла сохрани diff (через систему контроля версий) и согласуй изменения с эксплуатацией/ответственными за конфиги.

4. **Документация и rollback:**
   - Обнови/создай `docs/configs/passport.md` с описанием:
    - поля `IsBalast`,  
     - общего поведения балласт/небалласт,  
     - порядка обновления конфигов (какие файлы менять, кто утверждает изменения),  
     - сценария отката (как восстановить конфиги из VCS/резервных копий, без отдельного миграционного скрипта).

**Проверка после Фазы 0 (обязательно реализуй и опиши в коде/доках):**

1. На выборке конфигов (минимум 2–3 файла) проверь валидность JSON и корректность проставления `IsBalast` (сравни с эталонным конфигом и планом).
2. Добавь/обнови unit‑тесты для парсинга конфигов (`Tests/Configs/CfgEditPassportTests.cs`), которые подтверждают:
   - корректное чтение конфигов с `IsBalast`,
   - обратную совместимость для конфигов без `IsBalast`.
3. Ручная проверка: на одном устройстве с `UseElis = true` и одном с `UseElis = false` убедись, что паспорт открывается в UI без ошибок после внесённых изменений в конфиг.

В конце работы выдай:
- список изменённых/созданных файлов,
- подтверждение прохождения тестов.

---

### Промпт 1 — Фаза 1: стратегия и контракты (бэкенд, выполнено)

Согласно п.11 плана эта фаза уже закрыта, повторная реализация не требуется. Используй собранный результат как базу для следующих этапов и при необходимости только проверяй артефакты.

**Что уже сделано:**

1. **Стратегии в `DocPassport`:**
   - Выделен интерфейс `IPassportQualityStrategy` и две реализации (`PassportQualityStrategy`, `PassportQualityStrategyElis`), выбор между ними происходит строго по `UseElis`.
   - `BuildQualityParameters*` теперь работают через стратегию, выдавая единый контракт для ELIS on/off, при этом `IsBalast` влияет только в ветке ELIS.

2. **DTO и контракты:**
   - `PassportQualityParameterSchema` и связанные модели/TS-типы расширены полями `IsBalast` и `ResultEditMode`.
   - JSON‑контракты сохраняют обратную совместимость: если новые поля отсутствуют, клиент читает данные по-старому.

3. **Тесты и снапшоты:**
   - Добавлены `PassportQualityStrategyTests`, покрывающие сценарии ELIS on/off и проверяющие порядок `MethodOptions` (`config → lab → manual`).
   - Сняты и задокументированы сравнения `PassportEditConfig` для устройств с `UseElis = false/true`, подтверждающие сохранение прежнего поведения при ELIS Off.

Если необходимо перепроверить состояние, прогоняй `dotnet test Tests/Services --filter PassportQuality` и сравнивай актуальные снапшоты с артефактами Фазы 1.

---

### Промпт 2 — Фаза 2: сохранение и история (бэкенд)

Нужно реализовать **Фазу 2 — сохранение и история (бэкенд)** согласно разделу 11 в `tech_debt/PASSPORT_QUALITY_FILLING_PLAN.md`.  
Фокус — `DocPassport.DocUpdate`, `BuildCorrectionData`, работа с `LabInfo` и историей полей.

**Сделай следующее:**

1. **Расширение DocUpdate / CorrectionData:**
   - В `DocPassport.DocUpdate(int id, Dictionary<string, object> values)` и `BuildCorrectionData` убедись, что:
     - `__history` корректно парсится в `Dictionary<string, List<FieldHistoryEntry>>`,
     - при `UseElis = false` история игнорируется (как сейчас), но поведение явно задокументировано/прологировано.
   - Проверь, что все нужные теги (`Metod`, `Value`, `PrintValue`, `History`) корректно маппятся в `CorrectionData.Values`.

2. **Балластные/небалластные параметры при сохранении:**
   - В `DocUpdate(string jsonData, QualityPassport? elisProtocol)` после десериализации `CorrectionData` выполняй мягкую валидацию:
     - для балластных параметров только проверяй инвариант `value.* == result.*`; при расхождении пиши `Warn`, но не исправляй payload (ошибка отправителя),
     - для небалластных параметров принимай `result.*` как основное значение, при необходимости добавляя запись `Manual overrides ELIS`, если ручное значение перекрывает последние данные ELIS.
   - В `AddOrUpdateLabInfo` корректно проставляй `ElisFilled` и источники на основании присланной истории; при `UseElis = false` явно логируй, что история игнорируется и используется старая модель.

3. **История методов и `LabInfo`:**
   - Сохраняй текущий поток `DocUpdate` → `AddOrUpdateLabInfo`: UI по‑прежнему отправляет `Tag = "Metod"` с сериализованным JSON, сервер просто обновляет `LabInfo`.
   - `method.<key>__history` остаётся единственным источником информации о происхождении метода; `BuildParameterMethod` объединяет данные конфигов и `LabInfo` без новых флагов, порядок `config → lab` сохраняется.

4. **Логирование и диагностика:**
   - Добавь подробные логи категории `PassportQuality`:
     - выбор стратегии (`PassportQualityStrategy`/`PassportQualityStrategyElis`),
     - случаи конфликта value/result для балластных параметров,
     - сценарии `Manual overrides ELIS`.

5. **Тесты DocUpdate:**
   - В `Tests/Services/Passport/DocUpdateTests.cs` добавь интеграционные тесты на основе `CorrectionData`:
     - сценарий: ELIS импортировал значение, пользователь изменил результат небалластного параметра вручную, затем пришёл новый протокол ELIS — значение пользователя сохраняется, история содержит обе записи;
     - сценарий балластного параметра, где приходит расхождение value/result — сервер фиксирует `Warn`, но не трогает payload;
     - сценарий создания ручного метода через `Tag = "Metod"` + историю, метод возвращается через `LabInfo` в том же формате, что и сейчас.

**Проверка после Фазы 2:**

1. Выполни `dotnet test Tests/Services/Passport/DocUpdateTests.cs` и убедись, что все тесты зелёные.
2. На тестовом стенде через UI повтори ключевые сценарии из раздела 9 (Manual overrides, ELIS import) и проверь:
   - логи категории `PassportQuality`,
   - корректность истории (`__history`) для value/result/method.
3. Отдельно проверь, что при `UseElis = false` сохранение паспорта работает по старой модели (без влияния новой логики истории).

В конце дай:
- список затронутых методов в `DocPassport`,
- краткое описание алгоритма для балласт/небалласт,
- примеры лог‑записей из реального прогона.

---

### Промпт 3 — Фаза 3: UI и UX (DocumentPassportEditor + PassportQualityTable)

Реализуй **Фазу 3 — UI и UX** из `tech_debt/PASSPORT_QUALITY_FILLING_PLAN.md` для `DocumentPassportEditor` и связанных компонентов.  
Основные файлы:  
- `TN_Doc/Client/document-editor/src/views/DocumentPassportEditor.vue`,  
- `TN_Doc/Client/document-editor/src/components/passport/PassportQualityTable.vue`,  
- `TN_Doc/Client/document-editor/src/composables/usePassportEditor.ts`,  
- `TN_Doc/Client/document-editor/src/composables/useFieldHistory.ts`,  
- потенциальные новые компоненты модалок.

**Нужно сделать:**

1. **Расширение usePassportEditor и стора:**
   - В `usePassportEditor.ts`:
     - добавь поддержку `isBalast` и `resultEditMode` в `qualityParameters`;
     - повесь watchers на `formData["value.<key>"]` и `formData["result.<key>"]`, вынеси общий `recalculateResult` с учётом балласт/небалласт.
   - В store документа реализуй методы `syncBallastParameter` и `markManualOverride`, которые одним вызовом обновляют значения, `__history`, `__elisFilled` и вспомогательные флаги для отправки в `DocUpdate`.

2. **Поведение таблицы PassportQualityTable:**
   - Для **балластных** параметров:
     - поле Result должно быть read‑only (без возможности прямого ввода),
     - при изменении measurement автоматически обновлять result + историю (через `bulkUpdateFields`),
     - ячейка Result должна выглядеть одинаково с небалластными: рендерится кнопка «Редактировать», но она находится в состоянии disabled (нельзя открыть модалку);
     - дополнительно можно отрисовывать иконку синхронизации с tooltip «Балластный параметр — результат синхронизирован с измерением».
   - Для **небалластных** параметров:
     - рядом с Result должна быть активная кнопка «Редактировать» → открывает модалку редактирования результата;
     - результирующее значение отображается в готовом к печати виде.

3. **Модалка редактирования результатов:**
   - Создай компонент (например) `ResultEditDialog.vue`:
     - селектор оператора (`менее`/`более`),
     - поле для числа (учёт локали: запятая как разделитель),
     - предпросмотр строки (`"менее 4,0"`).
   - При подтверждении:
     - обновляй `result.<key>`, а для балластных при необходимости и `value.<key>`,
     - заполняй `result.<key>__history` с `source = Manual` и `payloadType = ResultModal`.
   - Реализуй блокировку кнопки «Применить» при ошибках формата/диапазона.

4. **Модалка ручного метода:**
   - Создай `ManualMethodDialog.vue`:
     - поля: имя метода, `limitValueActivate`, `limitValue`, `limitValueString`, `isDefault`/чекбокс «Сделать основным».
   - Интегрируй в `PassportQualityTable`:
     - кнопка возле комбобокса метода → открытие модалки.
   - При сохранении:
     - добавляй метод в локальный `methodOptions`,
     - обновляй `store.formData["method.<key>"]` (JSON) и `method.<key>__history` (Source = Manual).

5. **Подсветки и предупреждения:**
   - В `useFieldHistory`/компонентах:
     - подсвечивай строки, если последняя запись `method.<key>__history` имеет `Source = Manual` или выбранный метод отсутствует в конфиге; показывай tooltip «ручной метод» / «нет в справочнике»;
     - выводи напоминание, если результат заполнен без выбранного метода, и баннер, если история указывает на метод, отсутствующий в текущем конфиге.

6. **UI/стили и локализация:**
   - Приведи модалки к стилю `material3.css`:
     - цвета, бордеры, тени, высоту инпутов (~35px).
   - Добавь строки в `TN_Doc/Client/document-editor/src/locales/ru.json` для:
     - подписей новых кнопок,
     - текстов подсказок и предупреждений.

7. **Тесты UI:**
   - Напиши unit‑тесты на `usePassportEditor`:
     - поведение для балластных/небалластных,
     - корректный пересчёт result,
     - установка флагов предупреждений.
   - Напиши компонентные тесты для модалок:
     - валидация ввода,
     - формирование итоговой строки,
     - запись истории.

**Проверка после Фазы 3:**

1. Выполни `npm run test -- usePassportEditor` и тесты модальных компонентов, убедись, что всё зелёное.
2. В Storybook или на тестовом стенде визуально проверь:
   - блокировку Result для балластных параметров,
   - работу модалок редактирования результата и ручного метода,
   - подсветку методов по истории и предупреждения о методах, отсутствующих в конфиге.
3. По сценариям 2–4 из раздела 9 прогони UI руками и сделай несколько скриншотов (для последующего использования в release notes).

В конце выдай:
- список новых/изменённых компонентов и composables,
- краткое описание UX‑поведения для балласт/небалласт,
- результат прогонов фронтовых тестов.

---

### Промпт 4 — Фаза 4: end‑to‑end проверки и документация

Реализуй **Фазу 4 — end‑to‑end проверки и документация** из `tech_debt/PASSPORT_QUALITY_FILLING_PLAN.md` для проекта `TN_Doc`.

**Сделай:**

1. **Документация:**
   - Обнови `docs/features/field-history.md`:
     - опиши источники (`Manual`, `ELIS`, `System`) и то, как история используется для measurement/result/method,
     - зафиксируй сценарий `Manual overrides ELIS`.
   - Обнови `docs/elis-summary.md`:
     - как ELIS взаимодействует с ручными правками,
     - приоритет последнего действия (ELIS или оператор).
   - В `docs/architecture/document-editor.md`:
     - добавь секцию про `DocumentPassportEditor`, таблицу качества, модалки,
     - опиши использование `isBalast` и `ResultEditMode`.

2. **Интеграционные и ручные сценарии:**
   - Для ключевых сценариев из раздела 9 плана (импорт ELIS, Manual overrides, создание ручного метода, печать) убедись, что:
     - на бэкенде есть unit/integration‑тесты (`DocPassport`, `PassportQualityStrategy`, `DocUpdate`),
     - остальная часть потока (UI и действия пользователя) покрыта ручным тестированием по чек‑листу.
   - Подготовь чек‑лист ручных сценариев для QA/эксплуатации с указанием:
     - шагов в UI,
     - ожидаемого результата,
     - стендов/устройств (ELIS on/off).

3. **Интеграционная матрица:**
   - На основе раздела 9 плана составь таблицу «сценарий ↔ тип проверки (unit/integration/manual) ↔ файл теста/чек‑лист».
   - Убедись, что для каждого сценария есть либо автоматический тест (unit/integration), либо явный ручной сценарий.

**Проверка после Фазы 4:**

1. Выполни `dotnet test` для всего решения и сохрани отчёты (артефакты CI или `docs/reports/passport_tests_<date>.md`).
2. Пройди чек‑лист ручного тестирования для стендов ELIS on/off и зафиксируй результаты (успех/замечания).
3. Проверь, что документация консистентна с фактическим поведением (история, приоритет ELIS vs Manual, модалки).

В конце предъяви:
- ссылки/пути к обновлённым докам,
- ссылку/файл с чек‑листом ручных сценариев и результатами прогона,
- краткий вывод по результатам авто‑ и ручных проверок.

---

### Промпт 5 — Фаза 5: регресс и релиз

Реализуй **Фазу 5 — регресс и релиз** из `tech_debt/PASSPORT_QUALITY_FILLING_PLAN.md` для проекта `TN_Doc`.

**Нужно:**

1. **Полный регрессионный прогон:**
   - Выполни:
     - `dotnet build TN_Doc.sln`,
     - `dotnet test /p:CollectCoverage=true` (зафиксируй уровень покрытия, особенно по изменённым областям),
     - `npm run build` в:
       - `TN_Doc/Client/statusbar`,
       - `TN_Doc/Client/document-editor`.
   - Зафиксируй результаты (логи/отчёты, особенно ошибки, если были).

2. **Смоук‑тесты на стендах:**
   - На стендах с `UseElis = false` и `UseElis = true`:
     - создай/открой паспорт, отредактируй measurement/result, сохрани, распечатай;
     - протестируй импорт ELIS → ручное редактирование → повторный импорт.
   - Собери результаты в `docs/release/passport_<version>.md` (или аналог):
     - список стендов/устройств,
     - краткий результат по каждому сценарию.

3. **Release notes и коммуникация:**
   - Подготовь release notes с описанием:
     - новых фич (балласт/небалласт, модалки, история),
     - изменения конфигов (`IsBalast`, скрипт миграции),
     - необходимых действий эксплуатаций (запуск миграции, проверка стендов),
     - известных ограничений/рисков.
   - Приложи скриншоты:
     - балластных строк с заблокированным Result,
     - модалок редактирования результата и метода.

**Проверка после Фазы 5:**

1. Подтверди, что:
   - все smoke‑сценарии с ELIS on/off прошли без блокеров,
   - CI прогон (build + tests + e2e) зелёный.
2. Зафиксируй итоговый статус релиза и известные ограничения в `docs/release/passport_<version>.md`.
3. Отрази подтверждение заказчика по UX (например, ссылкой на задачу/протокол встречи).

В конце выдай:
- ссылки на release notes и отчёт по регрессу,
- сводный статус по всем стендам/устройствам,
- список известных ограничений/рисков, если остались.


